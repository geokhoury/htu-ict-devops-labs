#!/bin/bash

# Loop over two arrays and find the difference between them.
# Store results of differences between the arrays in array3.

# Declare two arrays
array1=(blue olive yellow maroon)
array2=(reg green blue white)

# Get the 3rd element
echo ${array[2]}

# Get all elements of the array
echo ${array[@]}

# Get number of elements in the array
echo ${#array[@]}